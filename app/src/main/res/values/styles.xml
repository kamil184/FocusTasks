<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Widget.App.TimerProgressBar" parent="Widget.Material3.CircularProgressIndicator">
        <item name="trackThickness">4dp</item>
        <item name="trackCornerRadius">2dp</item>
        <item name="indicatorSize">@dimen/timer_size_big</item>
        <item name="trackColor">?attr/trackColor</item>
        <!--TODO: видно засечки на trackCornerRadius-->
    </style>

    <style name="ModalBottomSheetDialog" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <item name="backgroundTint">@android:color/white</item>
        <item name="background">@android:color/white</item>
    </style>
</resources>